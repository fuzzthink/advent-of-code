{
  "private": true,
  "scripts": {
    "start": "npx fable-splitter src/ --run -w",
    "fable": "npx fable-splitter src/ --run",
    "build": "npx fable-splitter src/ --run -o out/",
    "watch": "npx fable-splitter src/ --run -o out/ -w",
    "paket-i": "mono .paket/paket.exe install"
  },
  "dependencies": {
    "@babel/core": "^7.2.2",
    "fable-compiler": "^2.1.11",
    "fable-loader": "^2.1.2",
    "fable-splitter": "^2.1.2"
  }
}
